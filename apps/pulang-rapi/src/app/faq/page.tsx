import PageShell from "@/components/PageShell"; import SectionHeading from "@/components/SectionHeading"; import FAQAccordion from "@/components/FAQAccordion"; import CTA from "@/components/CTA"; import { faqGroups } from "@/content/faqs";
export default function FAQPage() { let counter = 0; return <PageShell><section className="page-intro"><div className="shell narrow"><p className="eyebrow">Pertanyaan umum</p><h1>Yang perlu Anda tahu sebelum memesan.</h1><p>Jawaban di bawah ini dibuat setepat mungkin. Untuk harga, area, dan bahan tertentu, konfirmasi terakhir tetap dilakukan melalui WhatsApp.</p></div></section><section className="section faq-page-section"><div className="shell faq-groups">{faqGroups.map((group) => { const items = group.items.map((item) => ({ ...item, q: item.q, id: `faq-${counter++}` })); return <section key={group.title} className="faq-group"><SectionHeading title={group.title} /><FAQAccordion items={items} /></section> })}</div></section><CTA title="Belum menemukan jawabannya?" text="Kirim pertanyaan Anda lewat WhatsApp. Kami akan menjawab berdasarkan kondisi pesanan yang sebenarnya." /></PageShell> }

