import { services } from "@/data/business"; import WhatsAppButton from "./whatsapp-button";
export default function ServiceMenu({ preview=false }:{preview?:boolean}) { const list=preview?services.slice(0,4):services; const groups=["Potong rambut","Cukur dan janggut","Perawatan","Paket"]; return <div className="service-list">{groups.map(group=>{const items=list.filter(s=>s.category===group); if(!items.length)return null; return <section key={group} className="pt-8 first:pt-0"><h3 className="mb-2 text-xs font-bold uppercase tracking-[.16em] text-tobacco">{group}</h3>{items.map(s=><div className="service-row" key={s.name}><div><h4 className="font-display text-[1.35rem] leading-tight">{s.name}</h4><p className="service-detail mt-2 max-w-lg text-sm leading-6 muted">{s.description} <span className="hidden sm:inline">{s.includes}.</span>{s.note&&<span className="block text-xs text-tobacco">{s.note}</span>}</p></div><div><div className="font-display text-xl">Rp{s.price.toLocaleString("id-ID")}</div><div className="mt-1 text-xs muted">{s.duration}</div></div><div className="service-cta"><WhatsAppButton label="Pilih" service={s.name} className="min-h-9 px-3 text-xs" /></div></div>)}</section>})}</div> }
