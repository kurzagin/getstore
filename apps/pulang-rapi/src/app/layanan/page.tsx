import Image from "next/image"; import PageShell from "@/components/PageShell"; import SectionHeading from "@/components/SectionHeading"; import CTA from "@/components/CTA"; import { services } from "@/content/services"; import { comparison } from "@/content/pricing"; import { whatsappUrl } from "@/lib/whatsapp";
export default function LayananPage() { return <PageShell><section className="page-intro"><div className="shell narrow"><p className="eyebrow">Layanan & harga</p><h1>Laundry yang bisa dijelaskan sebelum dititipkan.</h1><p>Setiap jenis layanan punya cara kerja, waktu, dan catatan yang berbeda. Pilih yang sesuai, lalu tanyakan hal yang masih perlu dipastikan.</p></div></section><section className="service-image-strip"><div className="shell service-image-layout"><Image className="section-photo" src="/images/folded-towels-linen.png" alt="Tumpukan handuk putih, kain biru, dan aksen kuning di rak laundry" width={1200} height={800} sizes="(max-width: 640px) 100vw, 55vw" /><div><p className="eyebrow">Tekstil rumah</p><h2>Dari pakaian harian sampai linen yang lebih besar.</h2><p>Jenis barang dan bahannya ikut menentukan cara kami menerima serta memproses pesanan.</p></div></div></section><section className="section service-catalog"><div className="shell"><div className="catalog-grid">{services.map((service, i) => <article className="catalog-item" id={service.slug} key={service.slug}><div className="catalog-number">0{i + 1}</div><div className="catalog-main"><p className="eyebrow">{service.label}</p><h2>{service.name}</h2><p className="catalog-desc">{service.description}</p><div className="catalog-columns"><div><h3>Termasuk</h3><ul>{service.includes.map((item) => <li key={item}>{item}</li>)}</ul></div><div><h3>Cocok untuk</h3><ul>{service.useCases.map((item) => <li key={item}>{item}</li>)}</ul></div></div><p className="catalog-note"><b>Catatan:</b> {service.notes.join(" ")}</p><a className="text-link" href={whatsappUrl(`Halo Pulang Rapi, saya ingin bertanya tentang ${service.name}. Lokasi saya di [alamat/area].`)}>Tanya layanan ini via WhatsApp ↗</a></div><div className="catalog-price"><span>{service.time}</span><strong>{service.price}</strong></div></article>)}</div></div></section><section className="section comparison-section"><div className="shell"><SectionHeading eyebrow="Ringkasnya" title="Tiga pilihan untuk cucian sehari-hari." /><div className="table-wrap"><table><thead><tr><th>Layanan</th><th>Yang termasuk</th><th>Proses</th><th>Harga mulai</th></tr></thead><tbody>{comparison.map((row) => <tr key={row.name}><td>{row.name}</td><td>{row.includes}</td><td>{row.process}</td><td>{row.price}</td></tr>)}</tbody></table></div></div></section><section className="section before-section"><div className="shell before-layout"><SectionHeading title="Sebelum menitipkan pakaian." intro="Beberapa informasi kecil membantu kami menangani pesanan dengan lebih tepat." /><div className="before-list">{["Kosongkan isi saku penting.", "Informasikan noda atau kondisi yang perlu diketahui.", "Sebutkan bahan yang sensitif warna atau mudah rusak.", "Konfirmasikan pakaian mahal atau sentimental.", "Periksa nota atau ringkasan pesanan digital."].map((x, i) => <div key={x}><span>0{i + 1}</span><p>{x}</p></div>)}</div></div></section><CTA title="Masih ragu memilih layanan?" text="Kirim foto atau jelaskan jenis pakaian Anda. Kami bantu cek pilihan yang paling masuk akal." /></PageShell> }
