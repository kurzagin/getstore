export default function SectionHeading({ eyebrow, title, children, align="left" }: { eyebrow?:string; title:string; children?:React.ReactNode; align?:"left"|"center" }) { return <div className={`${align === "center" ? "mx-auto text-center" : ""} max-w-2xl`}>{eyebrow&&<div className="eyebrow mb-3">{eyebrow}</div>}<h2 className="font-display text-3xl leading-[1.08] sm:text-4xl md:text-[3.3rem]">{title}</h2>{children&&<p className="mt-4 max-w-xl text-[15px] leading-7 muted">{children}</p>}</div> }
