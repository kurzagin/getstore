"use client";
import Link from "next/link"; import { useState } from "react"; import { business } from "@/data/business"; import { whatsappUrl } from "@/lib/whatsapp";
const links = [["Beranda","/"],["Layanan","/layanan"],["Barber","/barber"],["Galeri","/galeri"],["Tentang","/tentang"],["Kontak","/kontak"]];
export default function SiteHeader(){ const [open,setOpen]=useState(false); return <><div className="header-info border-b hairline text-[11px] font-semibold"><div className="site-container flex min-h-8 items-center justify-between gap-3"><span>Buka setiap hari, 10.00–21.00</span><span className="hidden sm:block">Walk-in boleh, booking lebih aman.</span></div></div><header className="site-header border-b hairline bg-bone/95"><div className="site-container header-shell flex h-[70px] items-center justify-between"><Link href="/" className="flex items-center gap-3" onClick={()=>setOpen(false)}><span className="flex h-9 w-9 items-center justify-center border border-ink text-[11px] font-bold tracking-[-.1em]">PK</span><span><span className="block font-display text-[1.18rem] leading-none">{business.name}</span><span className="mt-1 block text-[9px] font-bold uppercase tracking-[.19em] text-tobacco">{business.tagline}</span></span></Link><nav className="desktop-nav hidden items-center gap-5 lg:flex" aria-label="Navigasi utama">{links.slice(1).map(([label,href])=><Link key={href} href={href} className="text-[13px] font-semibold hover:text-copper">{label}</Link>)}<a className="btn btn-dark ml-2 min-h-10" href={whatsappUrl()} target="_blank" rel="noreferrer">Booking</a></nav><button className="menu-button flex h-11 w-11 items-center justify-center border border-ink lg:hidden" aria-expanded={open} aria-controls="mobile-nav" aria-label={open?"Tutup menu":"Buka menu"} onClick={()=>setOpen(!open)}>{open?"×":"≡"}</button></div>{open&&<nav id="mobile-nav" className="mobile-menu border-t hairline px-4 pb-5 pt-3 lg:hidden" aria-label="Navigasi mobile">{links.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)} className="block border-b hairline py-3 text-base font-semibold">{label}</Link>)}<a href={whatsappUrl()} target="_blank" rel="noreferrer" className="btn btn-dark mt-4 w-full">Booking via WhatsApp</a></nav>}</header></> }
