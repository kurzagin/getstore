import Image from "next/image"; import PageShell from "@/components/PageShell"; import SectionHeading from "@/components/SectionHeading"; import AreaChecker from "@/components/AreaChecker"; import CTA from "@/components/CTA"; import { areas, areaNotes, pickupRules } from "@/content/areas"; import { siteConfig } from "@/content/site";
export default function AreaPage() { return <PageShell><section className="page-intro"><div className="shell narrow"><p className="eyebrow">Area layanan</p><h1>Penjemputan dekat, jadwal dibicarakan.</h1><p>Kami melayani area di sekitar outlet. Karena wilayah dan kapasitas dapat berubah, alamat Anda selalu kami cek satu per satu.</p></div></section><section className="section area-detail"><div className="shell area-detail-grid"><div><SectionHeading title="Wilayah yang tercantum saat ini." intro="Daftar ini adalah contoh konten dan perlu diganti dengan wilayah operasional yang benar." /><div className="big-area-list">{areas.map((area, i) => <div key={area}><span>0{i + 1}</span><h2>{area}</h2></div>)}</div></div><aside className="outlet-note"><p className="eyebrow">Outlet</p><h2>{siteConfig.address}</h2><p>{siteConfig.hours}</p><div className="rule-list">{areaNotes.map((x) => <p key={x}>— {x}</p>)}</div></aside></div></section><section className="section pickup-section"><div className="shell pickup-layout"><SectionHeading title="Ketentuan penjemputan." /><div className="rule-list numbered">{pickupRules.map((x, i) => <p key={x}><span>0{i + 1}</span>{x}</p>)}</div></div></section><section className="checker-section"><div className="shell checker-layout"><div><SectionHeading eyebrow="Cek alamat" title="Tidak yakin masuk area?" intro="Tulis kecamatan atau patokan alamat. Situs ini akan menyiapkan pesan untuk kami, bukan menyimpan data Anda." /><Image className="section-photo area-photo" src="/images/pickup-handoff-neighborhood.png" alt="Serah terima kantong laundry berisi pakaian biru dan putih di depan rumah" width={1000} height={562} sizes="(max-width: 640px) 100vw, 48vw" /></div><AreaChecker /></div></section><CTA title="Kirim alamat, kami cek bersama." /></PageShell> }
