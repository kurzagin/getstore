import Image from "next/image"; import { Barber } from "@/data/business"; import WhatsAppButton from "./whatsapp-button";
export default function BarberProfile({ barber, compact=false }:{barber:Barber;compact?:boolean}){ return <article className={`grid gap-6 border-t hairline py-8 md:grid-cols-[220px_1fr] ${compact?"md:grid-cols-[150px_1fr] py-6":""}`}><div className="photo aspect-[4/5]"><Image src={barber.image} alt={`Potret ${barber.name}, barber Pangkas Kala`} fill sizes="(max-width:768px) 100vw, 220px" className="object-cover" /></div><div><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-display text-3xl">{barber.name}</h2><p className="mt-1 text-sm font-semibold text-tobacco">{barber.role}</p></div><span className="text-xs muted">{barber.experience}</span></div><p className="mt-5 max-w-xl text-sm leading-7 muted">{barber.bio}</p><div className="mt-5 flex flex-wrap gap-2">{barber.specialties.map(s=><span key={s} className="border hairline px-2.5 py-1 text-xs">{s}</span>)}</div><div className="mt-6 grid gap-1 text-xs"><span><strong>Gaya konsultasi:</strong> {barber.style}</span><span><strong>Jadwal:</strong> {barber.days}</span></div><WhatsAppButton label={`Booking dengan ${barber.name}`} barber={barber.name} className="mt-6" /></div></article> }
